= simple_form_for @user, html: { class: 'well form-horizontal' } do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :email, as: :email
    = f.input :password
    = f.input :password_verification

  .form-actions
    = f.button :submit
